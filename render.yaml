services:
  - type: web
    name: strikezone-bridge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn strikezone_bridge:app --host 0.0.0.0 --port 10000
    plan: free
